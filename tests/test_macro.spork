;; Test macro with hyphenated name
(ns tests.test-macro)

(defmacro my-when [test & body]
  `(if ~test (do ~@body) nil))

(deftest hyphenated-macro-runs-body-when-true
  (assert (= (my-when true 42) 42)))

(deftest hyphenated-macro-skips-body-when-false
  (assert (= (my-when false 42) nil)))
